import { EmissionUnitPipe } from './emission-unit.pipe';

describe('EmissionUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new EmissionUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
